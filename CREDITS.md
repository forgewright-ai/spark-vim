# spark-vim -- credits and licenses

The plugin's own code is MIT (`LICENSE`).

- vim -- https://www.vim.org -- Vim License (charityware). The editor this
  plugin lives in; its own vimscript runs it.
- spark -- https://github.com/forgewright-ai/spark -- MIT. The verb this
  plugin calls (`spark edit`).

Built with Claude.
